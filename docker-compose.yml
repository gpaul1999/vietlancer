services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: vietlancer
      POSTGRES_USER: vietlancer
      POSTGRES_PASSWORD: vietlancer
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
